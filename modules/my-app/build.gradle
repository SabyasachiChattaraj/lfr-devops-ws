packageRunBuild.enabled=false;

build.dependsOn(packageRunBuildLiferay)

packageRunBuildLiferay{

	doLast{
		copy {
			from 'build.liferay/'
			include '*.jar'
			into "${rootProject.buildDir}/docker/deploy"
		}
	}

}



